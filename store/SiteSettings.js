export const state = () => ({
  isExperienceActive: false
})

export const mutations = {
  SET_WORK_EXPERIENCE_STATUS(state, status) {
    state.isExperienceActive = status
  }
}

// export const actions = {

// }
